<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.centerm.fud_demo.dao.FileDao">
    <select id="getAllFileByUsername" parameterType="string" resultType="com.centerm.fud_demo.entity.File">
      SELECT fud.file.name,fud.file.size,fud.file.description,fud.file.create_time
      FROM file
      WHERE fud.file.user_id=(SELECT fud.user.id FROM fud.user WHERE fud.user.username=#{username})
    </select>
</mapper>